---
const { title, description } = Astro.props;
---

<header class="container">
  <h1>{title}</h1>
  <p>{description}</p>
</header>

<style lang="scss">
  h1 {
    margin: var(--size-5) 0 var(--size-3) 0;
    line-height: 0.9em;
  }

  p {
    margin: var(--size-3) 0;
  }
</style>
