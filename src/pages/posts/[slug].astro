---
import BlogPost from '../../layouts/BlogPost.astro';

export async function getStaticPaths({rss}) {
  let allPosts = Astro.fetchContent('../../posts/*.md');
  let allSortedPosts = allPosts.sort((a, b) => new Date(b.date) - new Date(a.date));

  rss({
    // The RSS Feed title, description, and custom metadata.
    title: 'Mykal.Codes',
    description: 'Mykal Machon\'s blog; for random writing',
    customData: `<language>en-us</language>`,
    // The list of items for your RSS feed, sorted.
    items: allSortedPosts.map(post => ({
      title: post.title,
      description: post.astro.html,
      link: `/posts/${post.slug}`,
      creator: 'Mykal Machon',
      pubDate: post.date,
    })),
    dest: "/feed/posts.xml",
  });

  return allSortedPosts.map((post) => {
    return {
      params: {slug: post.slug},
      props: {...post}
    }
  })
}

const { slug } = Astro.request.params;
const content = Astro.props;
---

<BlogPost content={content}>
  {content.astro.html}
</BlogPost>
<p>Post: {slug}</p>